import { ConfigurationDirective } from './configuration.directive';

describe('ConfigurationDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfigurationDirective();
    expect(directive).toBeTruthy();
  });
});
